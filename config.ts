export default {
    username: 'user1',
    password: 'pa55word'

    // This file should be either included in .gitignore and provided only to authorized users or in the future we can replace it with reading system variables
}